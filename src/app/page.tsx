import { FeaturedEvents, Hero } from '@/components';

const Home = (): React.JSX.Element => {
    return (
        <div>
            <Hero />
            <FeaturedEvents />
        </div>
    );
};
export default Home;
